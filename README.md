# mirumagency-tech-test


` git clone https://github.com/Waxoussou/mirumagency-tech-test.git `
` npm install `

To run the tests, in Package.json file, update test script to ` "test": "jest"` 
and run command `npm run test`
